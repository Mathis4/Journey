<resources>
    <!-- Autres styles -->

    <!-- Style pour le bouton personnalisé -->
    <style name="ButtonStyle">
        <item name="android:background">@drawable/fond_bouton</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">18sp</item>
        <item name="android:padding">12dp</item>
    </style>

    <style name="SpinnerItemStyle" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:background">#555555</item>
        <item name="android:textColor">#FFFFFF</item>
        <!-- Autres attributs de style selon vos besoins -->
    </style>
    <style name="MyButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/grey</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/caghen</item>
    </style>

</resources>
